node_modules/
.env
*.log

# OCR test fixtures are generated on the fly by test/gen_fixtures.py
test/fixtures/*.jpg
test/fixtures/manifest.json

# 바이럴 수집기 런타임 상태(중복 방지 기록) — 소스가 아니라 서버별 상태이므로 커밋하지 않음
data/*.json
!data/.gitkeep
